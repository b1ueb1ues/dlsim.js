
export class Action {
    static init (src) {
        function new_action (conf) {
            return new Action(conf);
        }
        return new_action;
    }
    constructor (conf) {

    }
}

data = {"timing":1, "duration":1, "label":"test"}

h1.timing
h1.duration
h1.label 

c1.timing 
c1.duration
c1.atype
